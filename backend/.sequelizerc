const { join } = require('path')

module.exports = {
  'config': join(__dirname, 'src','app',  'config', 'database.config.js'),
  'models-path': join(__dirname,'src', 'app', 'models'),
  'seeders-path': join(__dirname,'src', 'app', 'databases', 'seeders'),
  'migrations-path': join(__dirname, 'src', 'app', 'databases', 'migrations')
}
